# ==========================
# General Settings
# ==========================
# Change prefix from Ctrl+B to Ctrl+A
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# Mouse on
set -g mouse on

# Fix colors for Neovim (Crucial for CloudTop/SSH)
set -g default-terminal "screen-256color"
set -as terminal-features ",xterm-256color:RGB"

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on

# ==========================
# Navigation & Splits
# ==========================
# 'o' for horizontal, 'e' for vertical
unbind '"'
unbind %
bind o split-window -v -c "#{pane_current_path}"
bind e split-window -h -c "#{pane_current_path}"

# Seamless Vim/Tmux Navigation
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|l?n?vim?x?|fzf)(diff)?$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'

# Resize panes using Alt-direction (No prefix)
bind -n M-h resize-pane -L 5
bind -n M-j resize-pane -D 5
bind -n M-k resize-pane -U 5
bind -n M-l resize-pane -R 5

# ==========================
# Copy Mode
# ==========================
# System clipboard is handled by the terminal: Ctrl+Shift+C / Ctrl+Shift+V.
# Hold Shift while selecting with the mouse to bypass tmux and select natively.
setw -g mode-keys vi
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-selection-and-cancel

# ==========================
# System Bindings
# ==========================
# Reload config
bind r source-file ~/.tmux.conf \; display "Tmux Config Reloaded!"
# Kill window without confirmation
bind-key & kill-window

# Set options top
set-option -g status-position top

# ====================
# Plugins
# ====================
set -g @plugin 'catppuccin/tmux'
set -g @plugin 'tmux-plugins/tpm'

# Rounded window/tab separators. catppuccin v2.x builds the tab format from this
# when tpm sources the plugin (the `run` line below), so it must be set first.
# With "rounded", the plugin supplies its own left/middle/right separators.
set -g @catppuccin_window_status_style "rounded"

set -g @catppuccin_window_number_position "right"

set -g @catppuccin_window_default_fill "number"
set -g @catppuccin_window_default_text "#W"

set -g @catppuccin_window_current_fill "number"
set -g @catppuccin_window_current_text "#W"

set -g @catppuccin_status_modules_right "directory session"
set -g @catppuccin_status_left_separator  " "
set -g @catppuccin_status_right_separator ""
set -g @catppuccin_status_right_separator_inverse "no"
set -g @catppuccin_status_fill "icon"
set -g @catppuccin_status_connect_separator "no"

set -g @catppuccin_directory_text "#{pane_current_path}"

# ====================
# Catppuccin palette override — "Aubergine" (matches kitty's Ubuntu look)
# ====================
# The flavor theme sets every @thm_* with `set -ogq` (only-if-unset), so
# defining them here — before tpm sources the plugin below — wins without
# editing the plugin. Backgrounds are aubergine shades; @thm_mantle is the exact
# kitty background (#300A24) so the status bar blends into the terminal. Accents
# use Ubuntu orange + the Tango palette to tie in with kitty.
# (@thm_mauve drives the active window pill; @thm_green the session module.)

# Backgrounds / surfaces (dark -> light)
set -g @thm_crust      "#1f0518"
set -g @thm_mantle     "#300a24"
set -g @thm_bg         "#3d1530"
set -g @thm_surface_0  "#4a1f3c"
set -g @thm_surface_1  "#5c2a4b"
set -g @thm_surface_2  "#6e3559"
set -g @thm_overlay_0  "#804a68"
set -g @thm_overlay_1  "#935a78"
set -g @thm_overlay_2  "#a66a88"

# Text
set -g @thm_fg         "#ffffff"
set -g @thm_subtext_1  "#e8dce4"
set -g @thm_subtext_0  "#d4c2cf"

# Accents — Ubuntu orange + Tango
set -g @thm_mauve      "#e95420"
set -g @thm_red        "#ef2929"
set -g @thm_maroon     "#e0707c"
set -g @thm_peach      "#fcaf3e"
set -g @thm_yellow     "#fce94f"
set -g @thm_green      "#8ae234"
set -g @thm_teal       "#34e2e2"
set -g @thm_sky        "#5bc8e0"
set -g @thm_sapphire   "#5aa0d0"
set -g @thm_blue       "#729fcf"
set -g @thm_lavender   "#c0a8e8"
set -g @thm_rosewater  "#f0d5c8"
set -g @thm_flamingo   "#eec4b4"
set -g @thm_pink       "#e9a0c8"

run '~/.tmux/plugins/tpm/tpm'
